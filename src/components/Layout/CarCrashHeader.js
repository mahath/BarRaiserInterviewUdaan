
import { Fragment, useState } from "react";
import classes from "./CarCrashHeader.module.css";

const Header = props => {

    

    return <Fragment>
        <header className={classes.header}>
            <h1>Car Crash Details</h1>
        </header>
    </Fragment>
};

export default Header;